[package]
name = "cga-examples"
version = "0.1.0"
edition = "2021"
description = "Demo CLIs for the cga crates."

[dependencies]
cga-core = { path = "../cga-core" }
cga-gpu = { path = "../cga-gpu" }
mlx-rs = { workspace = true }

[lints]
workspace = true
