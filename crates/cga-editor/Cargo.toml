[package]
name = "cga-editor"
version = "0.1.0"
edition = "2021"
description = "CGS web live-preview editor server."

[dependencies]
cga-core = { path = "../cga-core" }
cga-gpu = { path = "../cga-gpu" }
mlx-rs = { workspace = true }
tiny_http = "0.12"

[lints]
workspace = true
